"use client";

const LandingHero: React.FC = () => {

  return (
    <section className="relative overflow-hidden bg-white">
      {/* Legacy LandingHero content intentionally removed. Use `@templates/Hero` instead. */}
    </section>
  );
};

export default LandingHero;
